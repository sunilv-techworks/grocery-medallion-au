[project]
name = "grocery-forecast"
version = "0.0.0"
description = "Demand forecasting model for fresh produce (placeholder)"
requires-python = ">=3.11,<3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
bypass-selection = true
